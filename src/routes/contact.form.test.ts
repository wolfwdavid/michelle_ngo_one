import { describe, it } from 'vitest';

describe('Contact form (Web3Forms)', () => {
	it.todo('form submits to Web3Forms endpoint');

	it.todo('shows success state on successful submission');

	it.todo('shows error state on failed submission');

	it.todo('shows error state with contactEmail fallback on network error');

	it.todo('honeypot field is present and hidden');
});
