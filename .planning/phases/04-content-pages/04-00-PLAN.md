---
phase: 04-content-pages
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/routes/about.load.test.ts
  - src/routes/press.load.test.ts
  - src/routes/resume.load.test.ts
  - src/routes/blog.load.test.ts
  - src/routes/contact.form.test.ts
  - src/lib/__tests__/richtext-video.test.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05]

must_haves:
  truths:
    - "All 6 test stub files exist and vitest discovers them"
    - "Each test file has at least one failing test describing expected behavior"
    - "Running vitest reports 6 test files with pending or failing tests"
  artifacts:
    - path: "src/routes/about.load.test.ts"
      provides: "Test stubs for About page server load and rendering"
      contains: "getPageBySlug"
    - path: "src/routes/press.load.test.ts"
      provides: "Test stubs for Press page query and year grouping"
      contains: "getPressItems"
    - path: "src/routes/resume.load.test.ts"
      provides: "Test stubs for Resume page structured data and PDF link"
      contains: "getResume"
    - path: "src/routes/blog.load.test.ts"
      provides: "Test stubs for Blog index, detail, and entries() prerendering"
      contains: "getBlogPosts"
    - path: "src/routes/contact.form.test.ts"
      provides: "Test stubs for Contact form Web3Forms submission and state transitions"
      contains: "web3forms"
    - path: "src/lib/__tests__/richtext-video.test.ts"
      provides: "Test stubs for INLINES.HYPERLINK video URL detection in Rich Text"
      contains: "INLINES"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 4 content pages, establishing the test-first contract for subsequent plans.

Purpose: Satisfies Nyquist compliance -- every plan task must have a targeted automated verify command pointing to an existing test file.
Output: 6 test stub files with failing tests that Plans 01-03 will make pass.
</objective>

<execution_context>
@C:/Users/Mkaru/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mkaru/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-pages/04-CONTEXT.md
@.planning/phases/04-content-pages/04-VALIDATION.md
@.planning/phases/04-content-pages/04-RESEARCH.md

<interfaces>
<!-- Existing test patterns to follow -->

From src/routes/category.load.test.ts (established pattern):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
vi.mock('$lib/contentful/queries', () => ({ ... }));
```

From src/lib/__tests__/contentful-queries.test.ts (established pattern):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
```

From src/lib/contentful/types.ts:
```typescript
export interface PressItem { title: string; source: string; url: string; date: string; }
export interface ResumeData { resumePdfUrl: string | null; experience: Array<{...}>; education: Array<{...}>; skills: string[]; }
export interface BlogPost { title: string; slug: string; body: Document; publishedDate: string; excerpt: string; coverImageUrl: string | null; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create page load test stubs (About, Press, Resume, Blog)</name>
  <files>src/routes/about.load.test.ts, src/routes/press.load.test.ts, src/routes/resume.load.test.ts, src/routes/blog.load.test.ts</files>
  <read_first>
    - src/routes/category.load.test.ts (established route test pattern with vi.mock)
    - src/routes/homepage.load.test.ts (another route test pattern)
    - src/lib/contentful/types.ts (types for mock data: PressItem, ResumeData, BlogPost)
    - src/lib/contentful/queries.ts (function signatures to mock)
  </read_first>
  <action>
    Create 4 test stub files following the established vi.mock pattern from category.load.test.ts.

    **src/routes/about.load.test.ts:**
    - Mock `$lib/contentful/queries` with `getPageBySlug` returning mock PageData
    - Test: "load function calls getPageBySlug with 'about'" -- import load from `./about/+page.server`, verify it calls `getPageBySlug('about')`
    - Test: "returns page data with title, body, photoUrl, seoDescription"
    - Test: "returns null gracefully when no page found"
    - All tests should use `it.todo()` or minimal stubs that will fail until Plan 01 creates the actual load function

    **src/routes/press.load.test.ts:**
    - Mock `$lib/contentful/queries` with `getPressItems` returning mock PressItem[]
    - Test: "load function calls getPressItems"
    - Test: "returns press items sorted by date descending"
    - Tests as `it.todo()` or stubs

    **src/routes/resume.load.test.ts:**
    - Mock `$lib/contentful/queries` with `getResume` returning mock ResumeData
    - Test: "load function calls getResume"
    - Test: "returns resume with experience, education, skills arrays"
    - Test: "includes resumePdfUrl"
    - Tests as `it.todo()` or stubs

    **src/routes/blog.load.test.ts:**
    - Mock `$lib/contentful/queries` with `getBlogPosts` and `getBlogPostBySlug`
    - Test: "blog index load calls getBlogPosts"
    - Test: "blog detail load calls getBlogPostBySlug with slug param"
    - Test: "blog detail entries() returns slugs from getBlogPosts"
    - Tests as `it.todo()` or stubs

    Use `it.todo('description')` for tests that depend on code not yet written. This lets vitest discover and report them without failing the suite.
  </action>
  <verify>
    <automated>npx vitest run src/routes/about.load.test.ts src/routes/press.load.test.ts src/routes/resume.load.test.ts src/routes/blog.load.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 route test stub files exist. Vitest discovers all of them. Tests are marked as todo, not blocking the suite.</done>
</task>

<task type="auto">
  <name>Task 2: Create contact form and Rich Text video test stubs</name>
  <files>src/routes/contact.form.test.ts, src/lib/__tests__/richtext-video.test.ts</files>
  <read_first>
    - src/lib/__tests__/contentful-queries.test.ts (established lib test pattern)
    - src/lib/contentful/richtext.ts (current renderRichText function to test)
    - src/lib/utils/video.ts (parseVideoUrl for reference)
  </read_first>
  <action>
    **src/routes/contact.form.test.ts:**
    - Test: "form submits to Web3Forms endpoint" -- `it.todo()`
    - Test: "shows success state on successful submission" -- `it.todo()`
    - Test: "shows error state on failed submission" -- `it.todo()`
    - Test: "shows error state with contactEmail fallback on network error" -- `it.todo()`
    - Test: "honeypot field is present and hidden" -- `it.todo()`

    **src/lib/__tests__/richtext-video.test.ts:**
    - Import `renderRichText` from `$lib/contentful/richtext`
    - Mock `$lib/contentful/client` (same pattern as contentful-queries.test.ts)
    - Test: "renders Vimeo URL as data-video-facade placeholder" -- create a minimal Rich Text document with INLINES.HYPERLINK containing `https://vimeo.com/123456`, verify output contains `data-video-facade`
    - Test: "renders YouTube URL as data-video-facade placeholder" -- same with `https://www.youtube.com/watch?v=abc123`
    - Test: "renders regular hyperlink as normal anchor tag" -- verify `https://example.com` renders as `<a href=`
    - These tests CAN be written as real assertions (not todo) since they test the existing renderRichText function. They will fail (RED) until Plan 03 Task 1 adds the INLINES.HYPERLINK handler.

    Use `@contentful/rich-text-types` to construct minimal Document objects for Rich Text tests:
    ```typescript
    import { INLINES, BLOCKS, Document } from '@contentful/rich-text-types';
    ```
  </action>
  <verify>
    <automated>npx vitest run src/routes/contact.form.test.ts src/lib/__tests__/richtext-video.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Contact form test stubs exist (todo). Rich Text video tests exist with real assertions that will fail until INLINES handler is added. Vitest discovers all 6 test files.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose 2>&1 | grep -c "test file"` shows increased test file count
- All 6 new test files exist at expected paths
- Existing tests still pass (no regressions)
</verification>

<success_criteria>
- 6 test stub files exist: about.load, press.load, resume.load, blog.load, contact.form, richtext-video
- Vitest discovers all 6 files
- Existing test suite is not broken by new stubs
- Each stub has descriptive test names matching the behaviors Plans 01-03 will implement
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-00-SUMMARY.md`
</output>
