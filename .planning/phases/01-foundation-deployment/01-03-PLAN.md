---
phase: 01-foundation-deployment
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false
requirements: [TECH-04]

must_haves:
  truths:
    - "Site is live and accessible at the GitHub Pages URL"
    - "All pages load with correct CSS, JS, and font assets (no 404s in console)"
    - "Direct navigation to a sub-page URL works (e.g., /michelle_ngo_one/about/)"
    - "Navigation between pages works on the deployed site"
  artifacts: []
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GitHub Pages"
      via: "actions/deploy-pages@v4"
      pattern: "deploy-pages"
---

<objective>
Deploy the site to GitHub Pages and verify it works correctly with the project site base path.

Purpose: Validate the entire deployment pipeline end-to-end — the build, the GitHub Actions workflow, asset paths with /michelle_ngo_one base, and GitHub Pages serving. Catching deployment issues now prevents cascading problems in every future phase.
Output: A live, navigable site at wolfwdavid.github.io/michelle_ngo_one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-deployment/01-CONTEXT.md
@.planning/phases/01-foundation-deployment/01-01-SUMMARY.md
@.planning/phases/01-foundation-deployment/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to GitHub and trigger deployment</name>
  <files>none</files>
  <read_first>
    .github/workflows/deploy.yml (verify workflow exists and is correct),
    svelte.config.js (verify paths.base is set to '/michelle_ngo_one')
  </read_first>
  <action>
    1. Check if a git remote named `origin` exists pointing to `wolfwdavid/michelle_ngo_one`. If not:
       - Check if the GitHub repo exists: `gh repo view wolfwdavid/michelle_ngo_one 2>/dev/null`
       - If repo doesn't exist, inform the user they need to create it (or use `gh repo create wolfwdavid/michelle_ngo_one --public`)
       - Add the remote: `git remote add origin https://github.com/wolfwdavid/michelle_ngo_one.git`

    2. Ensure all work is committed to the `main` branch.

    3. Push to GitHub: `git push -u origin main`

    4. After push, check if GitHub Pages is enabled with Actions source:
       - Go to repo Settings > Pages > Source should be "GitHub Actions"
       - If gh CLI available: `gh api repos/wolfwdavid/michelle_ngo_one/pages` to check status
       - If Pages not configured, the user needs to enable it: Settings > Pages > Source > GitHub Actions

    5. Monitor the GitHub Actions workflow: `gh run list --limit 1` or check the Actions tab.

    6. Wait for the deployment to complete. Check deployment status: `gh run watch` or poll `gh api repos/wolfwdavid/michelle_ngo_one/pages`.
  </action>
  <verify>
    <automated>gh api repos/wolfwdavid/michelle_ngo_one/pages --jq '.status' 2>/dev/null || echo "Check https://github.com/wolfwdavid/michelle_ngo_one/actions manually"</automated>
  </verify>
  <acceptance_criteria>
    - Code is pushed to wolfwdavid/michelle_ngo_one main branch
    - GitHub Actions workflow has run (visible in Actions tab)
    - GitHub Pages shows status "built" or deployment succeeded
  </acceptance_criteria>
  <done>Code pushed to GitHub, Actions workflow triggered, and deployment completed successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployed site on GitHub Pages</name>
  <files>none</files>
  <action>
    Open https://wolfwdavid.github.io/michelle_ngo_one/ in a browser and verify:
    1. Homepage loads with "Michelle Ngo" hero text
    2. No 404 errors in browser console (F12 > Console)
    3. All 7 nav links navigate correctly with /michelle_ngo_one/ prefix in URL
    4. Direct URL access to https://wolfwdavid.github.io/michelle_ngo_one/about/ works (not 404)
    5. Mobile drawer works at narrow viewport
    6. Footer shows social icons and copyright
    7. Inter font is rendering (check DevTools > Computed)
    Present results to user for final approval.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://wolfwdavid.github.io/michelle_ngo_one/ 2>/dev/null || echo "Manual verification required"</automated>
  </verify>
  <done>User has verified the deployed site loads correctly with no broken assets, working navigation, and correct base path on GitHub Pages.</done>
</task>

</tasks>

<verification>
- Site accessible at wolfwdavid.github.io/michelle_ngo_one
- No 404 errors in browser console for CSS/JS/font assets
- Direct URL access to sub-pages works (e.g., /michelle_ngo_one/about/)
- Navigation works on deployed site
- Mobile drawer works on deployed site
- Font renders as Inter
</verification>

<success_criteria>
The site is live on GitHub Pages with all assets loading correctly, all routes navigable, responsive layout working, and no console errors. The deployment pipeline (push to main -> GitHub Actions build -> Pages deploy) is validated end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployment/01-03-SUMMARY.md`
</output>
