---
phase: 01-foundation-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - svelte.config.js
  - vite.config.ts
  - src/app.html
  - src/app.css
  - src/routes/+layout.js
  - static/.nojekyll
  - .github/workflows/deploy.yml
  - tsconfig.json
  - src/lib/config/navigation.ts
autonomous: true
requirements: [TECH-01, TECH-02, TECH-03, TECH-04]

must_haves:
  truths:
    - "Project builds successfully with npm run build"
    - "Build output contains static HTML files in build/ directory"
    - "Build output contains .nojekyll file"
    - "Tailwind CSS utility classes are processed and included in build output"
    - "Google Fonts (Inter) link is present in the HTML shell"
    - "GitHub Actions workflow exists for automated deployment"
  artifacts:
    - path: "svelte.config.js"
      provides: "SvelteKit config with adapter-static, paths.base, trailingSlash"
      contains: "adapter-static"
    - path: "vite.config.ts"
      provides: "Vite config with Tailwind v4 plugin"
      contains: "tailwindcss"
    - path: "src/app.css"
      provides: "Tailwind imports and custom theme (Inter font, accent color)"
      contains: "@import"
    - path: "src/app.html"
      provides: "HTML shell with Google Fonts preconnect and Inter font link"
      contains: "Inter"
    - path: "src/routes/+layout.js"
      provides: "Prerender export for static generation"
      contains: "prerender"
    - path: "static/.nojekyll"
      provides: "Prevents Jekyll processing on GitHub Pages"
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions workflow for Pages deployment"
      contains: "deploy-pages"
    - path: "src/lib/config/navigation.ts"
      provides: "Navigation items and social links data"
      contains: "navItems"
  key_links:
    - from: "svelte.config.js"
      to: "@sveltejs/adapter-static"
      via: "import and adapter() call"
      pattern: "adapter-static"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugin import"
      pattern: "tailwindcss"
    - from: "src/app.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: "@import.*tailwindcss"
---

<objective>
Scaffold a SvelteKit project with Svelte 5, configure Tailwind CSS v4, adapter-static for GitHub Pages, and set up the deployment pipeline.

Purpose: Establish the project foundation that all subsequent plans and phases build on. This is the greenfield scaffold — from zero to a building, styled, deployable SvelteKit project.
Output: A SvelteKit project that builds to static HTML with Tailwind v4 styling, Inter font, custom accent color theme, and a GitHub Actions deployment workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-deployment/01-CONTEXT.md
@.planning/phases/01-foundation-deployment/01-RESEARCH.md
@.planning/phases/01-foundation-deployment/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold SvelteKit project and configure build pipeline</name>
  <files>
    package.json,
    svelte.config.js,
    vite.config.ts,
    src/routes/+layout.js,
    static/.nojekyll,
    tsconfig.json
  </files>
  <read_first>
    .planning/phases/01-foundation-deployment/01-RESEARCH.md (code examples for svelte.config.js, vite.config.ts, +layout.js),
    CLAUDE.md (stack versions and "what NOT to use" guidance)
  </read_first>
  <action>
    1. Run `npx sv create . --template minimal --types ts` to scaffold the SvelteKit project in the current directory. If the CLI is interactive, select: minimal template, TypeScript, add tailwindcss. If tailwindcss add-on is not available during scaffold, install manually in step 2.

    2. Install required dependencies:
       ```
       npm install -D @sveltejs/adapter-static@^3.0.10
       ```
       Verify these are in package.json after scaffold: @sveltejs/kit@^2.59, svelte@^5.55, tailwindcss@^4.2, @tailwindcss/vite@^4.2. If tailwindcss or @tailwindcss/vite are missing, install them:
       ```
       npm install -D tailwindcss@^4.2.4 @tailwindcss/vite@^4.2.4
       ```

    3. Replace svelte.config.js with this exact content (per D-01, D-02, TECH-03, TECH-04):
       ```javascript
       import adapter from '@sveltejs/adapter-static';
       import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

       /** @type {import('@sveltejs/kit').Config} */
       const config = {
         preprocess: vitePreprocess(),
         kit: {
           adapter: adapter({
             pages: 'build',
             assets: 'build',
             fallback: '404.html',
             precompress: false,
             strict: true
           }),
           paths: {
             base: process.argv.includes('dev') ? '' : '/michelle_ngo_one'
           },
           trailingSlash: 'always'
         }
       };

       export default config;
       ```

    4. Replace vite.config.ts with this exact content (TECH-02):
       ```typescript
       import { sveltekit } from '@sveltejs/kit/vite';
       import tailwindcss from '@tailwindcss/vite';
       import { defineConfig } from 'vite';

       export default defineConfig({
         plugins: [
           tailwindcss(),
           sveltekit()
         ]
       });
       ```

    5. Create src/routes/+layout.js with this exact content (TECH-03):
       ```javascript
       export const prerender = true;
       ```

    6. Create an empty file at static/.nojekyll (TECH-04). The file must exist but can be empty.

    7. Verify the project builds: run `npm run build`. The build/ directory should contain index.html and a 404.html.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Desktop/hello_world/websites/michelle_ngo/one" && npm run build && test -f build/index.html && test -f build/404.html && test -f static/.nojekyll && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - svelte.config.js contains `import adapter from '@sveltejs/adapter-static'`
    - svelte.config.js contains `base: process.argv.includes('dev') ? '' : '/michelle_ngo_one'`
    - svelte.config.js contains `trailingSlash: 'always'`
    - svelte.config.js contains `fallback: '404.html'`
    - vite.config.ts contains `import tailwindcss from '@tailwindcss/vite'`
    - vite.config.ts contains `tailwindcss()` in plugins array
    - src/routes/+layout.js contains `export const prerender = true`
    - static/.nojekyll file exists
    - `npm run build` exits 0
    - build/index.html exists after build
  </acceptance_criteria>
  <done>SvelteKit project scaffolded with Svelte 5 + TypeScript, adapter-static configured for GitHub Pages project site, Tailwind v4 Vite plugin active, prerendering enabled, .nojekyll present, and project builds successfully to static HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Configure theme, fonts, navigation data, and deployment workflow</name>
  <files>
    src/app.html,
    src/app.css,
    src/lib/config/navigation.ts,
    .github/workflows/deploy.yml
  </files>
  <read_first>
    src/app.html (current state after scaffold),
    src/app.css (current state after scaffold),
    .planning/phases/01-foundation-deployment/01-RESEARCH.md (code examples for app.html, app.css, navigation.ts, deploy.yml),
    .planning/phases/01-foundation-deployment/01-UI-SPEC.md (typography, color, spacing specs)
  </read_first>
  <action>
    1. Replace src/app.html with this exact content (DES-05, D-07 Inter font):
       ```html
       <!doctype html>
       <html lang="en">
         <head>
           <meta charset="utf-8" />
           <link rel="icon" href="%sveltekit.assets%/favicon.png" />
           <link rel="preconnect" href="https://fonts.googleapis.com" />
           <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
           <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300..700&display=swap" rel="stylesheet" />
           <meta name="viewport" content="width=device-width, initial-scale=1" />
           %sveltekit.head%
         </head>
         <body data-sveltekit-preload-data="hover">
           <div style="display: contents">%sveltekit.body%</div>
         </body>
       </html>
       ```

    2. Replace src/app.css with this exact content (TECH-02, D-06 monochrome + accent, D-08 steel blue):
       ```css
       @import "tailwindcss";

       @theme {
         --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;
         --color-accent: #4A6FA5;
         --color-accent-hover: #3B5D8C;
       }
       ```
       Do NOT use @tailwind directives (v3 pattern). Do NOT create a tailwind.config.js file.

    3. Create src/lib/config/navigation.ts with this exact content (NAV-01 items, CONT-06 social links, D-11 flat nav):
       ```typescript
       import { base } from '$app/paths';

       export interface NavItem {
         label: string;
         href: string;
       }

       export interface SocialLink {
         label: string;
         href: string;
         icon: string;
       }

       export const navItems: NavItem[] = [
         { label: 'Home', href: `${base}/` },
         { label: 'Advertising', href: `${base}/advertising/` },
         { label: 'Film-TV', href: `${base}/film-tv/` },
         { label: 'UX Design', href: `${base}/ux-design/` },
         { label: 'Social & Transmedia', href: `${base}/social-transmedia/` },
         { label: 'Publishing', href: `${base}/publishing/` },
         { label: 'About', href: `${base}/about/` },
       ];

       export const socialLinks: SocialLink[] = [
         { label: 'IMDb', href: 'https://www.imdb.com/name/PLACEHOLDER/', icon: 'imdb' },
         { label: 'LinkedIn', href: 'https://www.linkedin.com/in/PLACEHOLDER/', icon: 'linkedin' },
         { label: 'Vimeo', href: 'https://vimeo.com/user2149742', icon: 'vimeo' },
         { label: 'YouTube', href: 'https://www.youtube.com/PLACEHOLDER', icon: 'youtube' },
       ];
       ```
       IMPORTANT: All hrefs use `${base}/path/` with trailing slash (matches trailingSlash: 'always' config). The `base` import is resolved at build time by SvelteKit.

    4. Create .github/workflows/deploy.yml with this exact content (TECH-04):
       ```yaml
       name: Deploy to GitHub Pages

       on:
         push:
           branches: ['main']
         workflow_dispatch:

       permissions:
         contents: read
         pages: write
         id-token: write

       concurrency:
         group: 'pages'
         cancel-in-progress: false

       jobs:
         build:
           runs-on: ubuntu-latest
           steps:
             - name: Checkout
               uses: actions/checkout@v4

             - name: Setup Node
               uses: actions/setup-node@v4
               with:
                 node-version: 22
                 cache: npm

             - name: Install dependencies
               run: npm ci

             - name: Setup Pages
               uses: actions/configure-pages@v4

             - name: Build
               run: npm run build

             - name: Upload artifact
               uses: actions/upload-pages-artifact@v3
               with:
                 path: 'build/'

         deploy:
           needs: build
           runs-on: ubuntu-latest
           environment:
             name: github-pages
             url: ${{ steps.deployment.outputs.page_url }}
           steps:
             - name: Deploy to GitHub Pages
               id: deployment
               uses: actions/deploy-pages@v4
       ```
       Create the .github/workflows/ directory if it does not exist.

    5. Verify the project still builds after all changes: run `npm run build`.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Desktop/hello_world/websites/michelle_ngo/one" && npm run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app.html contains `family=Inter:wght@300..700`
    - src/app.html contains `rel="preconnect" href="https://fonts.googleapis.com"`
    - src/app.css contains `@import "tailwindcss"`
    - src/app.css contains `--font-sans: 'Inter'`
    - src/app.css contains `--color-accent: #4A6FA5`
    - src/app.css contains `--color-accent-hover: #3B5D8C`
    - src/app.css does NOT contain `@tailwind`
    - No tailwind.config.js file exists in the project root
    - src/lib/config/navigation.ts contains `export const navItems`
    - src/lib/config/navigation.ts contains `export const socialLinks`
    - src/lib/config/navigation.ts contains `${base}/advertising/` (trailing slash)
    - src/lib/config/navigation.ts contains `vimeo.com/user2149742`
    - .github/workflows/deploy.yml contains `actions/deploy-pages@v4`
    - .github/workflows/deploy.yml contains `node-version: 22`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Inter font loaded via Google Fonts, Tailwind v4 theme configured with custom sans font and steel blue accent color, navigation data file defines all 7 nav items and 4 social links as single source of truth, GitHub Actions deployment workflow created.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- build/ directory contains index.html, 404.html
- static/.nojekyll exists
- svelte.config.js uses adapter-static with correct paths.base
- No Tailwind v3 patterns present (no tailwind.config.js, no @tailwind directives)
- navigation.ts exports navItems (7 items) and socialLinks (4 items)
- GitHub Actions workflow file exists
</verification>

<success_criteria>
The SvelteKit project is scaffolded, configured, and builds to static HTML. Tailwind v4 is active with Inter font and steel blue accent color. Navigation data is defined. GitHub Actions deployment workflow is ready. The project is a clean foundation for Plan 02 to build the UI components on top of.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployment/01-01-SUMMARY.md`
</output>
