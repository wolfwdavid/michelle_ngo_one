---
phase: 03-portfolio-video
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/routes/advertising/+page.svelte
  - src/routes/advertising/+page.server.ts
  - src/routes/advertising/[slug]/+page.svelte
  - src/routes/advertising/[slug]/+page.server.ts
  - src/routes/film-tv/+page.svelte
  - src/routes/film-tv/+page.server.ts
  - src/routes/film-tv/[slug]/+page.svelte
  - src/routes/film-tv/[slug]/+page.server.ts
  - src/routes/ux-design/+page.svelte
  - src/routes/ux-design/+page.server.ts
  - src/routes/ux-design/[slug]/+page.svelte
  - src/routes/ux-design/[slug]/+page.server.ts
  - src/routes/social-transmedia/+page.svelte
  - src/routes/social-transmedia/+page.server.ts
  - src/routes/social-transmedia/[slug]/+page.svelte
  - src/routes/social-transmedia/[slug]/+page.server.ts
  - src/routes/publishing/+page.svelte
  - src/routes/publishing/+page.server.ts
  - src/routes/publishing/[slug]/+page.svelte
  - src/routes/publishing/[slug]/+page.server.ts
  - src/routes/copywriting/+page.svelte
  - src/routes/copywriting/+page.server.ts
  - src/routes/copywriting/[slug]/+page.svelte
  - src/routes/copywriting/[slug]/+page.server.ts
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03, PORT-04, PORT-05, PORT-06]

must_haves:
  truths:
    - "Each of the 6 categories has a dedicated page showing all projects in a thumbnail grid"
    - "Category pages show 3-column grid (desktop), 2-column (tablet), 1-column (mobile)"
    - "Clicking a thumbnail on a category page opens the lightbox player"
    - "Each project has a detail page at /{category}/{slug}/ with video, metadata, and description"
    - "Dynamic [slug] routes export entries() for static prerendering"
    - "Copywriting route is created (did not exist before)"
    - "All links use base from $app/paths for GitHub Pages deployment"
  artifacts:
    - path: "src/routes/advertising/+page.server.ts"
      provides: "Category load function for advertising"
      exports: ["load"]
    - path: "src/routes/advertising/[slug]/+page.server.ts"
      provides: "Project detail load + entries for prerendering"
      exports: ["load", "entries"]
    - path: "src/routes/copywriting/+page.svelte"
      provides: "New copywriting category page"
      min_lines: 10
    - path: "src/routes/copywriting/[slug]/+page.server.ts"
      provides: "Copywriting project detail with entries"
      exports: ["load", "entries"]
  key_links:
    - from: "src/routes/advertising/+page.server.ts"
      to: "src/lib/contentful/queries.ts"
      via: "getProjects('advertisingProject')"
      pattern: "getProjects\\('advertisingProject'\\)"
    - from: "src/routes/advertising/[slug]/+page.server.ts"
      to: "src/lib/contentful/queries.ts"
      via: "getProjectBySlug + entries export"
      pattern: "export const entries"
---

<objective>
Build all 6 category pages with thumbnail grids and all 6 sets of project detail pages with dynamic [slug] routes. This is the bulk content delivery of the portfolio.

Purpose: Category pages are the primary browsing experience -- visitors see all projects in a discipline as a thumbnail grid with lightbox playback. Detail pages provide SEO-friendly, shareable URLs for individual projects with full case study content.
Output: 24 route files (6 categories x (page + server + [slug]/page + [slug]/server)).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-portfolio-video/03-CONTEXT.md
@.planning/phases/03-portfolio-video/03-RESEARCH.md
@.planning/phases/03-portfolio-video/03-UI-SPEC.md

@src/routes/advertising/+page.svelte
@src/lib/contentful/queries.ts
@src/lib/contentful/types.ts
@src/lib/config/categories.ts

<interfaces>
<!-- From Plan 01 -->

From src/lib/config/categories.ts:
```typescript
export interface CategoryConfig { name, slug, href, contentTypeId }
export const CATEGORIES: CategoryConfig[];
```

From src/lib/contentful/queries.ts:
```typescript
export async function getProjects(contentTypeId: ProjectContentTypeId): Promise<Project[]>;
export async function getProjectBySlug(contentTypeId: ProjectContentTypeId, slug: string): Promise<Project | null>;
```

From src/lib/components/VideoThumbnailCard.svelte:
```typescript
// Props: thumbnailUrl, title, videoUrl, onclick, href?
```

From src/lib/components/VideoLightbox.svelte:
```typescript
// Props: projects: Project[], currentIndex: number ($bindable), open: boolean ($bindable)
```

From src/lib/components/VideoFacade.svelte:
```typescript
// Props: videoUrl, thumbnailUrl, title, onclick?, inline?
```

From src/lib/components/Breadcrumb.svelte:
```typescript
// Props: items: Array<{ label: string; href?: string }>
```

From src/lib/components/SEO.svelte:
```typescript
// Props: title?, description?, image?, type?
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create category page and project detail templates for all 6 categories</name>
  <files>src/routes/advertising/+page.svelte, src/routes/advertising/+page.server.ts, src/routes/advertising/[slug]/+page.svelte, src/routes/advertising/[slug]/+page.server.ts, src/routes/film-tv/+page.svelte, src/routes/film-tv/+page.server.ts, src/routes/film-tv/[slug]/+page.svelte, src/routes/film-tv/[slug]/+page.server.ts, src/routes/ux-design/+page.svelte, src/routes/ux-design/+page.server.ts, src/routes/ux-design/[slug]/+page.svelte, src/routes/ux-design/[slug]/+page.server.ts, src/routes/social-transmedia/+page.svelte, src/routes/social-transmedia/+page.server.ts, src/routes/social-transmedia/[slug]/+page.svelte, src/routes/social-transmedia/[slug]/+page.server.ts, src/routes/publishing/+page.svelte, src/routes/publishing/+page.server.ts, src/routes/publishing/[slug]/+page.svelte, src/routes/publishing/[slug]/+page.server.ts, src/routes/copywriting/+page.svelte, src/routes/copywriting/+page.server.ts, src/routes/copywriting/[slug]/+page.svelte, src/routes/copywriting/[slug]/+page.server.ts</files>
  <read_first>src/routes/advertising/+page.svelte, src/lib/contentful/queries.ts, src/lib/contentful/types.ts, src/lib/config/categories.ts, src/lib/components/ContentfulImage.svelte, .planning/phases/03-portfolio-video/03-UI-SPEC.md</read_first>
  <action>
    All 6 categories follow identical patterns with only 3 values changing: `categoryName`, `categorySlug`, and `contentTypeId`. Build the advertising version first as the template, then replicate for all 6.

    **Category mappings:**
    | Category | slug | contentTypeId |
    |----------|------|---------------|
    | Advertising | advertising | advertisingProject |
    | Film & TV | film-tv | filmProject |
    | UX Design | ux-design | uxDesignProject |
    | Social & Transmedia | social-transmedia | socialTransmediaProject |
    | Publishing | publishing | publishingProject |
    | Copywriting | copywriting | copywritingProject |

    **IMPORTANT:** Create the `src/routes/copywriting/` directory and `src/routes/copywriting/[slug]/` directory -- these do NOT exist yet.

    ---

    **Category page `+page.server.ts` template** (e.g., `src/routes/advertising/+page.server.ts`):

    ```typescript
    import type { PageServerLoad } from './$types';
    import { getProjects } from '$lib/contentful/queries';

    export const load: PageServerLoad = async () => {
      const projects = await getProjects('advertisingProject');
      return {
        projects,
        categoryName: 'Advertising',
        categorySlug: 'advertising',
      };
    };
    ```

    Each category file replaces the contentTypeId, categoryName, and categorySlug values per the mapping above.

    ---

    **Category page `+page.svelte` template** (per D-13, D-14, D-15, PORT-01, PORT-02):

    ```svelte
    <script lang="ts">
      import { base } from '$app/paths';
      import SEO from '$lib/components/SEO.svelte';
      import Breadcrumb from '$lib/components/Breadcrumb.svelte';
      import VideoThumbnailCard from '$lib/components/VideoThumbnailCard.svelte';
      import VideoLightbox from '$lib/components/VideoLightbox.svelte';
      import type { Project } from '$lib/contentful/types';

      let { data } = $props();
      let lightboxOpen = $state(false);
      let lightboxIndex = $state(0);

      function openLightbox(index: number) {
        lightboxIndex = index;
        lightboxOpen = true;
      }
    </script>

    <SEO
      title="{data.categoryName} | Michelle Ngo"
      description="Michelle Ngo's {data.categoryName.toLowerCase()} portfolio"
    />

    <div class="mx-auto max-w-7xl px-4 py-8 lg:px-8">
      <Breadcrumb items={[
        { label: 'Home', href: `${base}/` },
        { label: data.categoryName },
      ]} />

      <h1 class="mt-4 text-2xl font-semibold text-gray-900 mb-6">{data.categoryName}</h1>

      {#if data.projects.length === 0}
        <div class="py-16 text-center">
          <h2 class="text-xl font-semibold text-gray-900">No projects yet</h2>
          <p class="mt-2 text-base text-gray-500">Projects for this category are being added. Check back soon.</p>
        </div>
      {:else}
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 lg:gap-6">
          {#each data.projects as project, i}
            <VideoThumbnailCard
              thumbnailUrl={project.thumbnailUrl}
              title={project.title}
              videoUrl={project.videoUrl ?? ''}
              onclick={() => openLightbox(i)}
              href="{base}/{data.categorySlug}/{project.slug}/"
            />
          {/each}
        </div>
      {/if}
    </div>

    <VideoLightbox
      projects={data.projects}
      bind:currentIndex={lightboxIndex}
      bind:open={lightboxOpen}
    />
    ```

    ---

    **Project detail `[slug]/+page.server.ts` template** (per D-11, PORT-05):

    ```typescript
    import type { PageServerLoad, EntryGenerator } from './$types';
    import { getProjects, getProjectBySlug } from '$lib/contentful/queries';
    import { error } from '@sveltejs/kit';

    export const entries: EntryGenerator = async () => {
      const projects = await getProjects('advertisingProject');
      return projects.map((p) => ({ slug: p.slug }));
    };

    export const load: PageServerLoad = async ({ params }) => {
      const project = await getProjectBySlug('advertisingProject', params.slug);
      if (!project) throw error(404, 'Project not found');
      return {
        project,
        categoryName: 'Advertising',
        categorySlug: 'advertising',
      };
    };
    ```

    Each category replaces contentTypeId, categoryName, categorySlug.

    ---

    **Project detail `[slug]/+page.svelte` template** (per D-12, PORT-05):

    ```svelte
    <script lang="ts">
      import { base } from '$app/paths';
      import SEO from '$lib/components/SEO.svelte';
      import Breadcrumb from '$lib/components/Breadcrumb.svelte';
      import VideoFacade from '$lib/components/VideoFacade.svelte';

      let { data } = $props();
      const p = data.project;
    </script>

    <SEO
      title="{p.title} | Michelle Ngo"
      description={p.description || `${p.title} - Michelle Ngo's ${data.categoryName.toLowerCase()} work`}
      image={p.thumbnailUrl ?? ''}
    />

    <div class="mx-auto max-w-7xl px-4 py-8 lg:px-8">
      <Breadcrumb items={[
        { label: 'Home', href: `${base}/` },
        { label: data.categoryName, href: `${base}/${data.categorySlug}/` },
        { label: p.title },
      ]} />

      <div class="mx-auto max-w-[800px]">
        <h1 class="mt-4 text-2xl font-semibold text-gray-900 mb-4">{p.title}</h1>

        {#if p.videoUrl}
          <VideoFacade
            videoUrl={p.videoUrl}
            thumbnailUrl={p.thumbnailUrl}
            title={p.title}
            inline={true}
          />
        {/if}

        <div class="mt-6 grid grid-cols-1 sm:grid-cols-2 gap-x-8 gap-y-3">
          {#if p.role}
            <div>
              <dt class="text-sm font-semibold uppercase text-gray-500">Role</dt>
              <dd class="text-base text-gray-900">{p.role}</dd>
            </div>
          {/if}
          {#if p.client}
            <div>
              <dt class="text-sm font-semibold uppercase text-gray-500">Client</dt>
              <dd class="text-base text-gray-900">{p.client}</dd>
            </div>
          {/if}
          {#if p.agency}
            <div>
              <dt class="text-sm font-semibold uppercase text-gray-500">Agency</dt>
              <dd class="text-base text-gray-900">{p.agency}</dd>
            </div>
          {/if}
          {#if p.year}
            <div>
              <dt class="text-sm font-semibold uppercase text-gray-500">Year</dt>
              <dd class="text-base text-gray-900">{p.year}</dd>
            </div>
          {/if}
          {#if p.productionType}
            <div>
              <dt class="text-sm font-semibold uppercase text-gray-500">Production Type</dt>
              <dd class="text-base text-gray-900">{p.productionType}</dd>
            </div>
          {/if}
          {#if p.platform}
            <div>
              <dt class="text-sm font-semibold uppercase text-gray-500">Platform</dt>
              <dd class="text-base text-gray-900">{p.platform}</dd>
            </div>
          {/if}
          {#if p.publisher}
            <div>
              <dt class="text-sm font-semibold uppercase text-gray-500">Publisher</dt>
              <dd class="text-base text-gray-900">{p.publisher}</dd>
            </div>
          {/if}
        </div>

        {#if p.description}
          <p class="mt-6 text-base leading-relaxed text-gray-700">{p.description}</p>
        {/if}
      </div>
    </div>
    ```

    Use `<dl>` wrapping the metadata key-value pairs for semantic HTML.

    ---

    **For all 6 categories, create 4 files each (24 total)**, substituting the category-specific values. The structure is identical -- only the contentTypeId, categoryName, and categorySlug strings change.

    **CRITICAL:** All `href` values MUST use `{base}/` prefix. The site deploys to `/michelle_ngo_one`.
    **CRITICAL:** Every `[slug]/+page.server.ts` MUST export `entries` for static prerendering.
    **CRITICAL:** Use `$props()` not `export let`. Use `onclick` not `on:click`.
  </action>
  <verify>
    <automated>npx svelte-check --threshold warning 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/copywriting/+page.svelte exists (new route created)
    - src/routes/copywriting/+page.server.ts contains `getProjects('copywritingProject')`
    - src/routes/copywriting/[slug]/+page.server.ts contains `export const entries`
    - src/routes/advertising/+page.server.ts contains `getProjects('advertisingProject')`
    - src/routes/advertising/[slug]/+page.server.ts contains `export const entries: EntryGenerator`
    - src/routes/advertising/[slug]/+page.server.ts contains `getProjectBySlug('advertisingProject'`
    - src/routes/film-tv/+page.server.ts contains `getProjects('filmProject')`
    - src/routes/ux-design/+page.server.ts contains `getProjects('uxDesignProject')`
    - src/routes/social-transmedia/+page.server.ts contains `getProjects('socialTransmediaProject')`
    - src/routes/publishing/+page.server.ts contains `getProjects('publishingProject')`
    - All 6 category +page.svelte files contain `VideoThumbnailCard`
    - All 6 category +page.svelte files contain `VideoLightbox`
    - All 6 category +page.svelte files contain `Breadcrumb`
    - All 6 category +page.svelte files contain `{base}/`
    - All 6 [slug]/+page.svelte files contain `VideoFacade`
    - All 6 [slug]/+page.svelte files contain `inline={true}` (detail page plays inline)
    - No file contains `on:click` (Svelte 4 syntax)
    - No file contains `export let` (Svelte 4 prop syntax)
    - No category page contains placeholder text "Content coming soon"
  </acceptance_criteria>
  <done>All 6 category pages display project thumbnail grids with lightbox, all 6 categories have dynamic [slug] project detail pages with video facade, metadata, and description, copywriting route created, all routes prerenderable with entries()</done>
</task>

</tasks>

<verification>
- `npx svelte-check --threshold warning` passes with no errors
- All 24 route files created
- Copywriting route exists at src/routes/copywriting/
- Every [slug]/+page.server.ts exports entries function
- All pages use base path correctly
</verification>

<success_criteria>
- 6 category pages each show thumbnail grid with play overlays and lightbox
- 6 sets of project detail pages with video, metadata, and description
- Copywriting route created (was missing)
- Empty state shown for categories with no CMS content
- All dynamic routes prerenderable via entries() export
- All internal links use base path
</success_criteria>

<output>
After completion, create `.planning/phases/03-portfolio-video/03-04-SUMMARY.md`
</output>
