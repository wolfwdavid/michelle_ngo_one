---
phase: 03-portfolio-video
plan: 05
type: execute
wave: 4
depends_on: [03-03, 03-04]
files_modified:
  - src/routes/film-tv/filmography/+page.svelte
  - src/routes/film-tv/filmography/+page.server.ts
  - src/lib/components/FilmographyTable.svelte
autonomous: false
requirements: [VID-04]

must_haves:
  truths:
    - "Filmography page at /film-tv/filmography/ displays a table of film credits"
    - "Table shows Year, Title, Role, and Production Type columns"
    - "Table has zebra striping and hover row highlighting"
    - "On mobile, table collapses to card layout"
    - "Title column links to project detail page if project exists"
    - "All Phase 3 pages render correctly and build succeeds"
  artifacts:
    - path: "src/routes/film-tv/filmography/+page.server.ts"
      provides: "Filmography data from film projects"
      exports: ["load"]
    - path: "src/routes/film-tv/filmography/+page.svelte"
      provides: "Filmography page with table component"
      min_lines: 10
    - path: "src/lib/components/FilmographyTable.svelte"
      provides: "Responsive table/card component for filmography data"
      min_lines: 40
  key_links:
    - from: "src/routes/film-tv/filmography/+page.server.ts"
      to: "src/lib/contentful/queries.ts"
      via: "getProjects('filmProject')"
      pattern: "getProjects\\('filmProject'\\)"
    - from: "src/lib/components/FilmographyTable.svelte"
      to: "$app/paths"
      via: "base path for project links"
      pattern: "import.*base.*from.*\\$app/paths"
---

<objective>
Build the filmography/credits page and run a final verification checkpoint across all Phase 3 work.

Purpose: The filmography completes VID-04 -- a structured credits list for Film-TV work. The checkpoint ensures all 16 requirements are met before marking the phase complete.
Output: 1 filmography page with responsive table, 1 verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-portfolio-video/03-CONTEXT.md
@.planning/phases/03-portfolio-video/03-RESEARCH.md
@.planning/phases/03-portfolio-video/03-UI-SPEC.md

@src/lib/contentful/queries.ts
@src/lib/contentful/types.ts

<interfaces>
From src/lib/contentful/types.ts:
```typescript
export interface Project {
  title: string;
  slug: string;
  description: string;
  thumbnailUrl: string | null;
  videoUrl: string | null;
  featured: boolean;
  sortOrder: number;
  role?: string;
  year?: number;
  productionType?: string;
}
```

From src/lib/contentful/queries.ts:
```typescript
export async function getProjects(contentTypeId: ProjectContentTypeId): Promise<Project[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filmography page with responsive table component</name>
  <files>src/routes/film-tv/filmography/+page.server.ts, src/routes/film-tv/filmography/+page.svelte, src/lib/components/FilmographyTable.svelte</files>
  <read_first>src/lib/contentful/queries.ts, src/lib/contentful/types.ts, src/routes/film-tv/+page.svelte, .planning/phases/03-portfolio-video/03-UI-SPEC.md</read_first>
  <action>
    **1. Create `src/routes/film-tv/filmography/+page.server.ts`** (per D-16, VID-04):

    ```typescript
    import type { PageServerLoad } from './$types';
    import { getProjects } from '$lib/contentful/queries';

    export const load: PageServerLoad = async () => {
      const projects = await getProjects('filmProject');
      // Sort by year descending for chronological filmography
      const sorted = [...projects].sort((a, b) => (b.year ?? 0) - (a.year ?? 0));
      return { projects: sorted };
    };
    ```

    **2. Create `src/routes/film-tv/filmography/+page.svelte`**:

    ```svelte
    <script lang="ts">
      import { base } from '$app/paths';
      import SEO from '$lib/components/SEO.svelte';
      import Breadcrumb from '$lib/components/Breadcrumb.svelte';
      import FilmographyTable from '$lib/components/FilmographyTable.svelte';

      let { data } = $props();
    </script>

    <SEO
      title="Filmography | Michelle Ngo"
      description="Michelle Ngo's filmography - a complete list of film and television credits including role, year, and production type."
    />

    <div class="mx-auto max-w-7xl px-4 py-8 lg:px-8">
      <Breadcrumb items={[
        { label: 'Home', href: `${base}/` },
        { label: 'Film & TV', href: `${base}/film-tv/` },
        { label: 'Filmography' },
      ]} />

      <h1 class="mt-4 text-2xl font-semibold text-gray-900 mb-6">Filmography</h1>

      {#if data.projects.length === 0}
        <div class="py-16 text-center">
          <h2 class="text-xl font-semibold text-gray-900">No credits yet</h2>
          <p class="mt-2 text-base text-gray-500">Filmography entries are being added. Check back soon.</p>
        </div>
      {:else}
        <FilmographyTable projects={data.projects} />
      {/if}
    </div>
    ```

    **3. Create `src/lib/components/FilmographyTable.svelte`** (per D-16, UI-SPEC filmography):

    Props: `projects: Project[]`

    **Desktop (lg+): HTML table**
    ```html
    <div class="hidden lg:block overflow-x-auto">
      <table class="w-full text-left">
        <thead>
          <tr class="border-b-2 border-[#E5E7EB]">
            <th class="py-3 pr-4 text-sm font-semibold uppercase tracking-wider text-gray-500 w-20">Year</th>
            <th class="py-3 pr-4 text-sm font-semibold uppercase tracking-wider text-gray-500">Title</th>
            <th class="py-3 pr-4 text-sm font-semibold uppercase tracking-wider text-gray-500">Role</th>
            <th class="py-3 text-sm font-semibold uppercase tracking-wider text-gray-500">Production Type</th>
          </tr>
        </thead>
        <tbody>
          {#each projects as project, i}
            <tr class="border-b border-[#E5E7EB] {i % 2 === 1 ? 'bg-gray-50' : ''} hover:bg-gray-100 transition-colors">
              <td class="py-3 pr-4 text-sm font-semibold text-gray-900">{project.year ?? '---'}</td>
              <td class="py-3 pr-4">
                <a href="{base}/film-tv/{project.slug}/" class="text-base text-gray-900 hover:text-[#4A6FA5]">{project.title}</a>
              </td>
              <td class="py-3 pr-4 text-sm text-gray-500">{project.role ?? '---'}</td>
              <td class="py-3 text-sm text-gray-500">{project.productionType ?? '---'}</td>
            </tr>
          {/each}
        </tbody>
      </table>
    </div>
    ```

    **Mobile (< lg): Card layout**
    ```html
    <div class="lg:hidden space-y-4">
      {#each projects as project}
        <div class="rounded border border-[#E5E7EB] p-4">
          <a href="{base}/film-tv/{project.slug}/" class="text-base font-semibold text-gray-900 hover:text-[#4A6FA5]">{project.title}</a>
          <div class="mt-2 grid grid-cols-2 gap-2 text-sm">
            <div><span class="font-semibold text-gray-500">Year:</span> <span class="text-gray-900">{project.year ?? '---'}</span></div>
            <div><span class="font-semibold text-gray-500">Role:</span> <span class="text-gray-900">{project.role ?? '---'}</span></div>
            <div class="col-span-2"><span class="font-semibold text-gray-500">Type:</span> <span class="text-gray-900">{project.productionType ?? '---'}</span></div>
          </div>
        </div>
      {/each}
    </div>
    ```

    Import `base` from `$app/paths` and `Project` type from `$lib/contentful/types`.
  </action>
  <verify>
    <automated>npx svelte-check --threshold warning 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/film-tv/filmography/+page.server.ts contains `getProjects('filmProject')`
    - src/routes/film-tv/filmography/+page.server.ts contains `.sort(` (year descending sort)
    - src/routes/film-tv/filmography/+page.svelte contains `Filmography`
    - src/routes/film-tv/filmography/+page.svelte contains `FilmographyTable`
    - src/routes/film-tv/filmography/+page.svelte contains `No credits yet` (empty state)
    - src/lib/components/FilmographyTable.svelte contains `<table`
    - src/lib/components/FilmographyTable.svelte contains `<thead`
    - src/lib/components/FilmographyTable.svelte contains `bg-gray-50` (zebra striping)
    - src/lib/components/FilmographyTable.svelte contains `hover:bg-gray-100`
    - src/lib/components/FilmographyTable.svelte contains `lg:hidden` (mobile card layout)
    - src/lib/components/FilmographyTable.svelte contains `hidden lg:block` (desktop table)
    - src/lib/components/FilmographyTable.svelte contains `{base}/film-tv/` (linked titles)
    - All files use $props() not export let
  </acceptance_criteria>
  <done>Filmography page displays responsive table (desktop) / cards (mobile) of film credits sorted by year descending, with linked titles to project detail pages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all Phase 3 pages render correctly</name>
  <files>none</files>
  <read_first>none</read_first>
  <action>
    Human verification checkpoint. Run `npm run dev` and visually verify all Phase 3 pages.

    **What was built:** Complete portfolio and video system: homepage with hero + 6 category sections + accordion expand + about snippet + press highlights, 6 category pages with thumbnail grids and lightbox, 6 sets of project detail pages, filmography page, and video lightbox with keyboard navigation.

    **How to verify:**
    1. Run `npm run dev` and open http://localhost:5173/michelle_ngo_one/
    2. **Homepage:** Verify "Michelle Ngo" heading, tagline, 6 category sections (may show empty states if no CMS content). Click a category heading to expand/collapse. Verify only one expands at a time.
    3. **Category page:** Click "See all" or navigate to /advertising/. Verify thumbnail grid layout (3 cols on desktop). Click a thumbnail to open lightbox.
    4. **Lightbox:** Verify dark overlay, centered video player, close (X, Escape, backdrop click), prev/next arrows, keyboard navigation (arrow keys, Tab focus cycling).
    5. **Project detail:** Navigate to a project detail page. Verify breadcrumb, video facade (click to play inline), metadata fields, description.
    6. **Filmography:** Navigate to /film-tv/filmography/. Verify table layout on desktop, card layout on mobile (resize browser).
    7. Run `npm run build` to verify all routes prerender successfully (no missing entries errors).

    **Resume signal:** Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm run build completes without errors
    - User confirms homepage, category pages, detail pages, lightbox, and filmography render correctly
  </acceptance_criteria>
  <done>User has visually verified all Phase 3 pages and confirmed the build succeeds</done>
</task>

</tasks>

<verification>
- `npx svelte-check --threshold warning` passes
- `npm run build` succeeds (all routes prerender)
- All Phase 3 requirement IDs covered across plans 01-05
</verification>

<success_criteria>
- Filmography page shows structured table of film credits
- Table is responsive (table on desktop, cards on mobile)
- Build succeeds with all routes prerendering
- User visually confirms all Phase 3 pages work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-portfolio-video/03-05-SUMMARY.md`
</output>
